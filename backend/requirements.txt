# Runtime
fastapi==0.136.3
uvicorn[standard]==0.49.0
groq==1.4.0
duckdb==1.5.3
pyarrow==14.0.2
python-dotenv==1.0.1

# Data tooling — used by the sampler and the test fixtures, not the request path
pandas==2.2.2

# Dev / CI
pytest==7.4.4
httpx==0.28.1
ruff==0.6.9
