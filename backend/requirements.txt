# Runtime
fastapi==0.136.3
uvicorn[standard]==0.49.0
groq==1.4.0
duckdb==1.5.3
python-dotenv==1.0.1

# Dev / CI
pytest==7.4.4
httpx==0.28.1
ruff==0.6.9

# Deliberately absent: pandas, pyarrow, numpy.
#
# The request path reads DuckDB results with fetchall() rather than fetchdf(),
# the sampler uses DuckDB's COPY, and the test fixture is written with DuckDB —
# so none of them are needed. Dropping them removes ~100 MB from the image and,
# more importantly, removes pyarrow's NumPy C-ABI coupling, which broke CI when
# an unpinned NumPy 2.x resolved against a pyarrow built for NumPy 1.x.
